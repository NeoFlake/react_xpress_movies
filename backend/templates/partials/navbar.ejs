<link rel="stylesheet" href="../../css/navbar.css" />

<nav class="navbar fixed-top navbar-expand-lg bg-body-tertiary">
    <div class="container-fluid">
        <a class="navbar-brand" href="/homepage"><img src="../../img/logo-site.png" alt="XpressMovies" width="100"
                height="70" /></a>
        <div class="collapse navbar-collapse">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <% if(navbar.isAdmin) { %>
                        <a class="nav-link active <%= (navbar.currentRoute === '/administration') ? 'disabled' : '' %>"
                            aria-current="page" href="/administration">Administration</a>
                        <% } else if(navbar.favoris> 0) { %>
                            <a class="nav-link active position-relative <%= (navbar.currentRoute === '/favoris') ? 'disabled' : '' %>"
                                aria-current="page" href="/favoris">Mes
                                favoris<span
                                    class="position-absolute top-30 start-90 translate-middle badge rounded-pill bg-info"
                                    style="font-size: 0.6rem; padding: 0.25em 0.4em;">
                                    <%= navbar.favoris %>
                                        <span class="visually-hidden">nombres de mes favoris</span>
                                </span></a>
                            <% } %>
                </li>
                <li class="nav-item">
                    <a class="nav-link active <%= (navbar.currentRoute === '/profile') ? 'disabled' : '' %>"
                        href="/profile">Mon profil</a>
                </li>
            </ul>
            <div class="me-3">
                <%= `${navbar.lastname} ${navbar.firstname}` %>
            </div>
                <button class="btn btn-outline-info" type="submit" onclick="window.location.href='/logout'">Se déconnecter</button>
        </div>
    </div>
</nav>
<div class="top-page">
</div>